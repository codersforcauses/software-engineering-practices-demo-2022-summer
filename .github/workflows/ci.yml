name: Python package

on:
  pull_request:
  workflow_dispatch:
    inputs:
      logLevel:
        description: "Log level"
        required: false
        default: "warning"

jobs:
  test_github_action:
    name: "Just testing"
    runs-on: ubuntu-latest
    steps:
      - name: "List directory structure"
        run: ls